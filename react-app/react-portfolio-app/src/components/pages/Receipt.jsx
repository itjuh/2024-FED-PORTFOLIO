import { ReceiptBackground } from "../layout/ReceiptBackground";

export function Receipt(){
    // receipt background(배경형태)
    // receipt main > header + body + footer
    return(<div>
        <ReceiptBackground />
    </div>);
}